mayra-sp1
/*const express = require('express');
const {chatController} = require('../controller/chatController');
const chatRoutes = express.Router();


// 
// router.get('/conversations/:userId', chatController.getConversations);
// // chatRoutes.post('/send', new chatController().sendMessage );
// // chatRoutes.get('/messages/:userId1/:userId2', new chatController().getMessages);
// // // chatRoutes.get('/users', async (req, res) => {
// // //     try {
// // //         const users = await User.find(); // Certifique-se de ter o schema de User configurado
// // //         res.status(200).json(users);
// // //     } catch (error) {
// // //         res.status(500).send("Erro ao buscar usuários.");
// // //     }
// // // });


// module.exports = chatRoutes;
